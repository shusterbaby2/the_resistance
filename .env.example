# Copy to .env and fill in. The CLI loads .env automatically.
# Needed only for Claude seats; local Ollama models need no key.
ANTHROPIC_API_KEY=sk-ant-...
# Uncomment if Ollama is not on the default http://localhost:11434
# OLLAMA_HOST=http://localhost:11434
